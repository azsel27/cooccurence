#OUTLINE

#process anchor word dataset
    #extract word_index and word
    #create hashmap of word to word_index

#process post dataset
    #list of tuples (user hash, post text)
    #for each post, create a list for each anchor word
    #count occurences of anchor words in a post in the list
    #create per post co-occurence matrix
        #iterate through counts in nested loop
        #multiply counts together for the cell in the matrix

#create hashmap from user hash to matrix

#for each post
    #create_or_get(user hash) matrix  m
    #add counts of post matrix to m

#for each user
    #scale matrix so all non-dupe values sum to 10 (full matrix sums to 20)
        # calc total co-occurence weight of full matrix = w
            # w = matrix.sum()
        # matrix = matrix / w
        # matrix = 20* matrix
    # add Lap(10/epsilon) (for now, eps=5, so Lap(2)) to every cell

# sum all matrices into aggregrate matrix

# find top 20 edge/edge weights


#Improvements/TODO
    #performance could certainly be better. this is something like O((# anchors)^2(#posts))